using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class TestOutSpace : MonoBehaviour
{
    public float refreshPosTime = 2;
    // Start is called before the first frame update
    void Start()
    {
        //GameObject monster = MyUtils.LoadModelPrefab("Enemy001");
        //Debug.Log("monster="+ monster);
    }

    // Update is called once per frame
    //void Update()
    //{
    //    refreshPosTime = -Time.deltaTime;
    //    if (refreshPosTime < 0)
    //    {
    //        Logger.PrintColor("yellow", this.gameObject.name+".pos=" + this.transform.position);
    //        refreshPosTime = 2;
    //    }
    //}
}

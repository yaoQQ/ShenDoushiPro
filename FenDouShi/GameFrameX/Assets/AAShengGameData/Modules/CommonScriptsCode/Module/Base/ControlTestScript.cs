using System;
using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class ControlTestScript : MonoBehaviour
{
    // Start is called before the first frame update
    void Start()
    {
        //ControlManager.Instance.init();

        //Type test = typeof(TestControl);
        //TestControl tt = (TestControl)Activator.CreateInstance(test);
        //tt.test2();

        //TestControl.Instance.test2();
        //ControlManager.Instance.clear();

    }

    // Update is called once per frame
    void Update()
    {

    }
}
